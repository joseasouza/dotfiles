- defaults:
    link:
      relink: true

- link:
    ~/.zshrc: .zshrc
    ~/.oh-my-zsh:
      create: true
      glob: true
      path: ohmyzsh/*

- shell:
  - [git submodule update --init --recursive, Installing submodules]
